### Go ###
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# We don't need to check in vendor code because we use Artifactory module proxy
vendor/

### Go Patch ###

/Godeps/

data

.vscode

bin/
pgdata/